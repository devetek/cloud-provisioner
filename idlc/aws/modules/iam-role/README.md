## Description

This module creates an IAM Role with an optional inline policy and attaches managed policies to the role. It is designed to be reusable and configurable for various use cases where an IAM Role is required.